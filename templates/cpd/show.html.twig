{% extends 'base_dash.html.twig' %}

{% block title %}Cpd{% endblock %}

{% block content %}
    <h1>Cpd</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cpd.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cpd.name }}</td>
            </tr>
            <tr>
                <th>Latitud</th>
                <td>{{ cpd.latitude }}</td>
            </tr>
            <tr>
                <th>Longitud</th>
                <td>{{ cpd.longitude }}</td>
            </tr>
            <tr>
                <th>Dirección</th>
                <td>{{ cpd.address }}</td>
            </tr>
        </tbody>
    </table>

    <div class="text-right mt-5">
        <a href="{{ path('cpd_index') }}">Volver</a>
        <a class="btn btn-warning ml-2" href="{{ path('cpd_edit', {'id': cpd.id}) }}"><i class="far fa-edit"></i> Editar</a>
    </div>

{% endblock %}
